import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {
    height: '100%',
    display: 'flex',
    flexDirection: 'column',
  },
  body: {
    overflow: 'auto',
    flexGrow: 1,
  },
});
